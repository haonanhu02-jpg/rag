# R5 synthetic grounded-answer dataset

`grounded-answer-v1.jsonl` is a small, repository-authored contract dataset. It contains no copied
documents and is used only for deterministic status, refusal and citation checks. Results produced
with `FakeModelRuntime` are implementation evidence, not a real-model quality claim.
