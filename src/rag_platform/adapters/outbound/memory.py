"""In-memory outbound adapters for deterministic tests."""  from __future__ import annotations  from contextlib import AbstractContextManager  from rag_platform.domain.entities import KnowledgeBase from rag_platform.domain.identifiers import KnowledgeBaseId, TenantId   class InMemoryKnowledgeBaseRepository:     def __init__(self) -> None:         self._items: dict[tuple[TenantId, KnowledgeBaseId], KnowledgeBase] = {}      def add(self, knowledge_base: KnowledgeBase) -> None:         key = (knowledge_base.tenant_id, knowledge_base.id)         if key in self._items:             raise ValueError("knowledge base already exists")         self._items[key] = knowledge_base      def get(         self, tenant_id: TenantId, knowledge_base_id: KnowledgeBaseId     ) -> KnowledgeBase | None:         return self._items.get((tenant_id, knowledge_base_id))   class InMemoryTransaction(AbstractContextManager[None]):     def __init__(self) -> None:         self.committed = False         self.rolled_back = False      def __enter__(self) -> None:         return None      def commit(self) -> None:         self.committed = True      def rollback(self) -> None:         self.rolled_back = True      def __exit__(self, exc_type: object, exc: object, traceback: object) -> None:         if exc_type is None:             self.commit()         else:             self.rollback()   class InMemoryTransactionManager:     def transaction(self) -> InMemoryTransaction:         return InMemoryTransaction()   class InMemoryObjectStore:     def __init__(self) -> None:         self._objects: dict[tuple[TenantId, str], bytes] = {}      def put(self, *, tenant_id: TenantId, key: str, value: bytes) -> None:         self._objects[(tenant_id, key)] = value      def get(self, *, tenant_id: TenantId, key: str) -> bytes | None:         return self._objects.get((tenant_id, key))   class InMemoryMessageQueue:     def __init__(self) -> None:         self.messages: list[tuple[str, str]] = []      def publish(self, topic: str, message_id: str) -> None:         self.messages.append((topic, message_id))   class HealthySearchIndex:     def healthcheck(self) -> bool:         return True